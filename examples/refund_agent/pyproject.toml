[project]
name = "deliberate-refund-example"
version = "0.0.1"
description = "Reference LangGraph agent using the Deliberate SDK"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.3,<0.5",
]
